# Token Tracking

Prefer file handoff over pasted context. Each role receives only the canonical
artifact paths it needs. Summaries belong in `ACTIVITY_LOG.jsonl` or structured
results, not in parent-session memory.
