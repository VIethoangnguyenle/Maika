# profiles/

Execution & setup config:

- `execution-mode.yaml` — tier micro-loop per platform (SP1b): subagent / fresh-session / inline-reload.
- `agent-memory-mcp-only-setup.md` — profile setup agent-memory MCP-only (không bundle backend).
