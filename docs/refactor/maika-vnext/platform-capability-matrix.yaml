version: 1

platforms:
  claude-code:
    subagent_spawn:
      supported: true
      evidence: ".maika/profiles/execution-mode.yaml:3 — tier `subagent` (Claude); Task tool"
      verified_at: 2026-07-10
    fresh_session:
      supported: true
      evidence: "`claude --help` → `-p, --print  Print response and exit` — CLI print mode chạy session mới không tương tác (cùng pattern agy -p / codex exec)."
      verified_at: 2026-07-10
    hook_pre_tool_use:
      supported: true
      evidence: ".maika/hooks/claude-code/settings.json:1"
      verified_at: 2026-07-10
    hook_session_start:
      supported: true
      evidence: "Platform hỗ trợ SessionStart hook event — verify trực tiếp trong live Claude Code session 2026-07-10 (SessionStart:startup hooks fire). Lưu ý: config Maika tại .maika/hooks/claude-code/settings.json hiện CHƯA wire event này."
      verified_at: 2026-07-10
    model_selection:
      supported: true
      evidence: "Claude Code Agent/Task tool nhận param `model` (sonnet|opus|haiku...) per-dispatch; agent definition .claude/agents/*.md hỗ trợ frontmatter `model:` — verify trực tiếp trong live session 2026-07-10. (Grep cli/platforms/claude_code.py không ra chỉ chứng minh adapter Maika chưa dùng, không phải platform thiếu capability.)"
      verified_at: 2026-07-10
  codex:
    subagent_spawn:
      supported: false
      evidence: "Codex exec không hỗ trợ invoke_subagent tool."
      verified_at: 2026-07-10
    fresh_session:
      supported: true
      evidence: ".maika/profiles/execution-mode.yaml:4"
      verified_at: 2026-07-10
    hook_pre_tool_use:
      supported: true
      evidence: ".maika/hooks/codex/hooks.json:1"
      verified_at: 2026-07-10
    hook_session_start:
      supported: false
      evidence: "Config Maika (.maika/hooks/codex/hooks.json) không wire event này; capability phía platform CHƯA xác minh độc lập — giữ false cho đến khi có evidence platform-level (R4)."
      verified_at: 2026-07-10
    model_selection:
      supported: true
      evidence: "codex exec --help hiển thị `-m, --model <MODEL>`"
      verified_at: 2026-07-10
  antigravity:
    subagent_spawn:
      supported: true
      evidence: "Hỗ trợ qua invoke_subagent tool."
      verified_at: 2026-07-10
    fresh_session:
      supported: true
      evidence: ".maika/profiles/execution-mode.yaml:4"
      verified_at: 2026-07-10
    hook_pre_tool_use:
      supported: true
      evidence: ".maika/hooks/antigravity/hooks.json:5 — ghi rõ ràng buộc version (hooks chỉ fire từ 2.0.1 + Tools v4.3.5)"
      verified_at: 2026-07-10
    hook_session_start:
      supported: false
      evidence: "Config Maika (.maika/hooks/antigravity/hooks.json) không wire event này; capability phía platform CHƯA xác minh độc lập — giữ false cho đến khi có evidence platform-level (R4)."
      verified_at: 2026-07-10
    model_selection:
      supported: true
      evidence: "agy --help hiển thị `--model`"
      verified_at: 2026-07-10
